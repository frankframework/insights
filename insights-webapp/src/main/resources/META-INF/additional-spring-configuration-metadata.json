{
  "properties": [
    {
      "name": "insights.webhook.secret",
      "type": "java.lang.String",
      "description": "Shared secret used to verify GitHub webhook deliveries via HMAC-SHA256 (X-Hub-Signature-256 header). Must be set to the same value configured in the frankframework repository webhook settings."
    }
  ]
}
