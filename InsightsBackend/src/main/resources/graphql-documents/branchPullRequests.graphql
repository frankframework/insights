query branchPullRequests($branchName: String!, $after: String) {
	repository(owner: "frankframework", name: "frankframework") {
		pullRequests(first: 100, baseRefName: $branchName, after: $after) {
			edges {
				node {
					id
					number
					title
					state
					url
					mergedAt
					labels(first: 10) {
						edges {
							node {
								name
							}
						}
					}
					milestone {
						title
					}
					closingIssuesReferences(first: 10) {
						edges {
							node {
								number
								subIssues (first:10) {
									edges{
										node {
											number
										}
									}
								}
							}
						}
					}
				}
			}
			pageInfo {
				hasNextPage
				endCursor
			}
		}
	}
}
